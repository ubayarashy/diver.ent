[phases.setup]
nixPkgs = [
  "php84",
  "php84Packages.composer",
  "php84Extensions.gd",
  "nodejs_22"
]

[phases.build]
cmds = [
  "composer install --no-interaction --optimize-autoloader",
  "npm install",
  "npm run build"
]